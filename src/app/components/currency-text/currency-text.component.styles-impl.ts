import {StyleSheet} from 'react-native';

export const currencyTextStyles = StyleSheet.create({
  currencyWrapper: {
    flexDirection: 'row',
    alignItems: 'flex-start',
  },
  integerStyle: {
    fontSize: 35,
  },
  decimalStyle: {
    fontSize: 20,
  },
});
